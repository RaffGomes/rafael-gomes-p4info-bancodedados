Definindo a tabela:

CREATE TABLE "aluno" (
  "ID" INTEGER NOT NULL,
  "nome" varchar(50) NOT NULL,
  "curso" varchar(20) NOT NULL,
  PRIMARY KEY ("ID" AUTOINCREMENT)
)

Adicionando dados na tabela (Create)

INSERT INTO aluno (nome,curso) VALUES
("Paulo Nickolas","INFORMÁTICA"),
("Rafael Gomes","QUÍMICA"),
("Ana Clara","EDIFICAÇÕES");

Lendo elementos da tabela (Read)

SELECT * FROM aluno;

Atualizando dados (Update)

UPDATE aluno set nome = "Paulo Rodrigo" where ID = 1;

Deletando valores (Delete)

DELETE FROM aluno WHERE ID = 3;
